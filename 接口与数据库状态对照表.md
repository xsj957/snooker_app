# 用户 B (Natural) — 接口与数据库状态对照表

> 采集时间：2026-08-30
> 用户信息：userId=`57d703dc-659a-4474-898e-b75efa1f2e0a`，phone=`13538506002`
> 设备：client_id=`5133079d-e0ab-4d33-a7f8-be84d8e29bb3`
> JWT Token：从 adb logcat 抓取，userId 与请求体一致

---

## 一、基础信息

| 字段 | 接口 `/mp/user/info` | 数据库 `ten_user` | 是否一致 |
|------|---------------------|-------------------|---------|
| userId | `57d703dc-...2e0a` | `57d703dc-...2e0a` | ✅ |
| nickname | Natural | Natural | ✅ |
| phone | (未返回) | 13538506002 | — |
| union_id | (未返回) | `oIp-Q5uHh1...b_0KE` | — |
| highestBreak | 91 | (需查 ten_score) | — |
| rating | -- | (需另查) | — |
| authType | 4 | — | — |

---

## 二、交手记录模块

### 2.1 对手与场次统计

| 指标 | 接口 `/mp/record/opponentStatistics` | 数据库 | 是否一致 | 备注 |
|------|--------------------------------------|--------|---------|------|
| 对手数 | **13** | `ten_inning` 去重对手 = **13** | ✅ | 完全一致 |
| 总场数 | compC=57, compR=58 (合计115) | `ten_inning` 总行数 = **121** |  | compC/compR 含义待确认（可能为胜/负局数，非场数） |
| 总局数 | framC=121, framR=58 | `ten_inning` 行数 = **121** | ⚠️ | framC=121 恰好等于总行数，framR 含义待确认 |

### 2.2 交手卡片（带视频过滤）

| 接口 `/mp/record/opponentListWithVideos` | 结果 | 分析 |
|------|------|------|
| 返回值 | `[]`（空列表） | 该接口只返回含**4种状态视频**（待解锁/下载中/制作中/已解锁）的对手。该用户在此 client_id 下视频多为已完成，可能时间范围或状态过滤导致返回空 |

### 2.3 场卡片 & 场统计

| 接口 | 返回值 | 数据库实际 | 问题 |
|------|--------|-----------|------|
| `/mp/record/competitionListWithVideos` | `code=1, "userA比赛信息为空"` | 121 场比赛 | ❌ **接口异常**，JWT userId 与请求体 userId 一致，可能是服务端逻辑问题 |
| `/mp/record/competitionStatistics` | `code=1, "userA比赛信息为空"` | 121 场比赛 | ❌ 同上 |

---

## 三、我的视频模块

### 3.1 视频数量对照

| 分类 | 接口返回 total | 数据库（该 client_id） | 数据库（所有 client_id） | 是否一致 |
|------|---------------|----------------------|------------------------|---------|
| **有效视频** readyV2 | **82** | VCS status=7 → **23** | VCS status=7 → **637** | ❌ 82 ≠ 23 也 ≠ 637 |
| **制作中** processingV2 | **0** | VCS status IN(1,2,3,5) → **0** | VCS status IN(1,2,3,5) → **92** | ⚠️ 该 client 确实为 0，与接口一致 |
| **失败视频** failedV2 | **0** | VCS status IN(4,6,9) → **0** | VCS status IN(4,6,9) → **84** | ️ 该 client 确实为 0，与接口一致 |

> **核心发现**：该 client_id（5133079d）是较新设备，VCS 仅 23 条记录（全部 status=7）。
> - processingV2=0 和 failedV2=0 与数据库一致 ✅
> - readyV2=82 与 VCS 的 23 条不符 ❌，差异原因待排查

### 3.2 readyV2=82 的来源分析

| 统计维度 | 值 | 说明 |
|---------|---|------|
| 该 client VCS status=7 | 23 | 该设备上已完成制作的视频 |
| 该 user video_order 数 | 281 | 总订单数 |
| 该 user 有 order 但无 VCS 记录的视频 | 276 | 未在该设备上产生 VCS 记录 |
| 该 user video_list status=1 | 276 | 已购买的视频 |
| **readyV2 接口返回** | **82** | 不等于上述任何值 |

> **推测**：readyV2 可能使用了不同的统计口径（如仅统计特定 category、特定时间范围、或 `makeBy` 字段过滤）。需要查看后端源码确认。

### 3.3 视频统计接口

| 指标 | 接口 `getVideoStatistics` | 数据库 | 是否一致 | 备注 |
|------|---------------------------|--------|---------|------|
| completedMakeCount | **82** | VL status=1 → 276 / VCS status=7(该client) → 23 | ❌ | 与 readyV2 相同值，可能复用同一查询 |
| failedMakeCount | **0** | VCS status IN(4,6)(该client) → 0 | ✅ | 该 client 确实无失败 |
| makingCount | **0** | VCS status IN(1,2,3,5)(该client) → 0 | ✅ | 该 client 确实无制作中 |
| breakPlusCount | **12** | VL 单杆20+ → 32 | ❌ | 口径不同 |
| highlightsCount | **4** | VL 精彩集锦 → 21 | ❌ | 口径不同 |
| mistakeCount | **14** | VL 失败集锦 → 41 | ❌ | 口径不同 |
| frameCount | **52** | VL 局视频 → 174 | ❌ | 口径不同 |

> **结论**：`getVideoStatistics` 的 category 计数与 `video_list` 的 category 数量差异巨大，可能仅统计**该用户在该设备上制作完成**的视频，而非全部已购买视频。

---

## 四、video_client_status 状态分布

### 4.1 该 client_id（5133079d）

| VCS status | 含义 | 记录数 | API 对应 | API 值 | 匹配 |
|-----------|------|--------|---------|--------|------|
| 7 | 制作完成 | 23 | readyV2 | 82 | ❌ |
| **合计** | | **23** | | | |

### 4.2 所有 client_id（该用户视频，通过 video_order 关联）

| VCS status | 含义 | distinct videos | 总记录数 | API 对应 |
|-----------|------|----------------|---------|---------|
| 2 | 原片已上传 | 36 | 49 | processingV2 |
| 3 | 下载中 | 26 | 32 | processingV2 |
| 4 | 下载失败 | 25 | 26 | failedV2 |
| 5 | 本地制作中 | 10 | 11 | processingV2 |
| 6 | 制作失败 | 23 | 57 | failedV2 |
| 7 | 制作完成 | 198 | 637 | readyV2 |
| 9 | 重新制作 | 1 | 1 | failedV2 |
| 12 | (未定义) | 4 | 6 | — |

> **注意**：同一视频在不同 client_id 下有独立的 VCS 记录。status=7 有 198 个不同视频但 637 条记录，说明平均每个视频在 3.2 个设备上有记录。

---

## 五、video_list 分类分布（该用户参与场次）

| category | status=0(待剪辑) | status=1(已购买) | status=2(已删除) | 合计 |
|----------|-----------------|-----------------|-----------------|------|
| 单杆20+ | 4 | 32 | 6 | 42 |
| 单杆50+ | 0 | 6 | 0 | 6 |
| 单杆80+ | 0 | 2 | 0 | 2 |
| 局视频 | 14 | 174 | 13 | 201 |
| 精彩集锦 | 0 | 27 | 0 | 27 |
| 失误集锦 | 0 | 41 | 8 | 49 |
| 精彩失败集锦 | 0 | 0 | 0 | 0 |
| **合计** | **18** | **276** | **31** | **327** |

---

## 六、video_order 订单

| 指标 | 值 |
|------|---|
| 总订单数 | 281 |
| 已支付 (pay_status=1) | 0 |
| 该 client_id 有 VCS 记录的视频数 | 5 |

> 所有订单 pay_status≠1，说明通过视频券或测试环境直接解锁，非现金支付。

---

## 七、对照结论

### ✅ 一致项

| 项目 | 接口 | 数据库 | 说明 |
|------|------|--------|------|
| 用户信息 | nickname=Natural | nickname=Natural | ✅ |
| 对手数 | opponentStatistics → 13 | ten_inning → 13 | ✅ |
| 该 client 制作中 | processingV2 → 0 | VCS IN(1,2,3,5) → 0 | ✅ 该 client 确实无制作中视频 |
| 该 client 失败 | failedV2 → 0 | VCS IN(4,6,9) → 0 | ✅ 该 client 确实无失败视频 |

### ❌ 需排查项

| 项目 | 接口值 | 数据库值 | 优先级 | 说明 |
|------|--------|---------|--------|------|
| **competitionListWithVideos** | code=1 报错 | 121 场 | **P0** | 所有 competition 接口均返回"userA比赛信息为空"，需排查后端 |
| **competitionStatistics** | code=1 报错 | 121 场 | **P0** | 同上 |
| **readyV2=82 vs VCS=23** | 82 | 23 (该client) | **P1** | 差异 59，需确认 readyV2 计数逻辑 |
| **getVideoStatistics category** | 与 VL 不符 | VL 各类别 >> API | **P2** | 统计口径不同，可能仅统计 App 本地制作 |
| **status=12** | 文档未定义 | 4 个视频 | **P2** | 新增状态，文档待补充 |

### ⚠️ 已知风险

1. **competition 接口报错**：JWT userId 与请求体 userId 一致，但接口返回"为空"，疑似服务端逻辑 Bug
2. **client_id 多实例**：同一用户在多个设备上有 VCS 记录，不同接口的统计口径需明确
3. **readyV2 计数口径不明**：82 不等于 VCS(23) 也不等于 VL(276)，需查看后端源码

---

## 八、接口-数据库字段速查表

| 接口 | 响应关键字段 | 对应数据库 | 关键表.字段 |
|------|------------|-----------|------------|
| `/mp/user/info` | data.userId, nickname, highestBreak | ten_user | id, nickname, phone |
| `/mp/record/opponentStatistics` | data.total(对手数) | ten_inning | left_id/right_id 去重计数 |
| `/mp/record/opponentListWithVideos` | data[].list(视频列表) | ten_inning + VCS | 对手聚合 + VCS status 过滤(0,1,2,3,5,7) |
| `/mp/record/competitionListWithVideos` | data[].list | ten_inning + VL + VCS | 场次 + 视频 + VCS 状态 |
| `/video/videoClient/myVideos/readyV2` | data.total, data.list | video_client_status | status=7 + client_id |
| `/video/videoClient/myVideos/processingV2` | data.total, data.list | video_client_status | status IN(1,2,3,5) + client_id |
| `/video/videoClient/myVideos/failedV2` | data.total, data.list | video_client_status | status IN(4,6,9) + client_id |
| `/video/videoClient/getVideoStatistics` | data.completedMakeCount 等 | video_list + VCS | category 分组 + VCS status |
| `video_order` | — | video_order | user_id → video_id → video_list |

---

## 九、测试排查速查流程

```
Bug: "视频状态显示不对"
  ↓
1. 抓包 → 看接口返回的 status/appVideoStatus 值
  ↓
2. 查 DB → video_client_status WHERE video_id=X AND client_id=Y
  ↓
3. 对比 → 接口返回的 status 是否 = DB 中的 status
  ↓
4. 如果不一致 → 接口层 Bug（后端查询逻辑问题）
   如果一致 → 前端层 Bug（App 渲染逻辑问题）
```
