# CLAUDE.md - 斯诺克大师 App 测试规范

## 项目概述

**产品名称**: 斯诺克大师 App (Snooker Master) v1.0.0 第一期
**国内版 + 海外版**：两个独立包，共用一套核心代码。除登录方式、包名、支付方式、服务器地区不同外，其他功能完全一致。

> 如无特别标注，文档内需求同时适用于国内及海外App。

### 国内 vs 海外 差异

| 维度 | 国内版 | 海外版 |
|------|--------|--------|
| 语言 | 中文 + 英文 | 多语言 |
| 支付(安卓) | 微信支付 | 多种支付方式 |
| 支付(iOS) | Apple IAP | Apple IAP |
| 服务器 | 国内服务器 | 海外服务器 |
| 登录(iOS) | 微信 + Apple | Apple + Google + Facebook |
| 登录(安卓) | 微信 | Google + Facebook + Apple |
| 推送(安卓) | 极光推送（华为/小米/OPPO/vivo） | 极光推送（FCM等） |

## 核心业务模块速查

### 1. 登录模块 (P0)
- 微信登录: UnionID 关联小程序↔App账号
- 数据同步: 仅**微信登录**与小程序同步数据；Apple/Google/Facebook登录仅App内数据，不涉及与小程序同步
- 游客模式: 未登录可浏览，操作需登录
- 计分系统: 同一二维码支持App/微信扫码登录
- 账号注销: 二次确认 + 小程序端同步"该账号已注销"

### 2. 视频制作模块 (P0) — 核心
- 流程: 比赛结束→工控机生成视频→用户解锁→App下载原片(腾讯云)→本地制作→成品缓存+存相册
- 视频类型: 单杆视频 / 局视频 / 精彩集锦 / 失误集锦
- 状态机: 见下方 database 章节的 video_client_status 表
- 过期: 48h未解锁 | 90d未制作 | 缓存清除
- 渲染: 单杆/局=片头片尾+俱乐部名称+头像昵称+比分条+Logo；集锦=+转场特效+标题+厚薄度杆速
- 量级: 10万视频/月
- **自动制作触发页面（仅4个）**: 首页 / 交手记录页 / 场详情页 / 我的视频页
- **NEW标记逻辑**: App本地制作完成未播放的视频，卡片**右上角**显示【new】（小写，替代原红点逻辑），点击播放后消失
- **自动退券触发条件**: 仅系统原因（腾讯云文件丢失/素材数据丢失）触发自动退券；用户原因（断网/杀App）**不退券**，保留任务+弹窗恢复
- **模拟自动退券方式**: 制作阶段→等下载完成后清除App缓存→重进App触发制作→素材丢失→自动退券；下载阶段需开发配合（失效URL）
- **相册权限弹窗**: 解锁视频成功后弹窗询问，第一期仅此一个时机
- **解锁后弹窗顺序**: 先弹"视频已解锁"确认弹窗→再弹"相册权限"弹窗

### 3. 我的视频模块 (P0)
- 分类筛选: 全部 / 单杆高分 / 精彩集锦 / 失误集锦 / 局视频（数量仅计已制作成功的视频）
- Tab命名: **有效视频/失效视频**（制作完成=有效，制作失败=无效，需点击Tab切换）
- 制作中横幅: 含下载中/制作中/中断视频
- 下载完成后支持预览(简单播放器)，制作完成后用专业播放器
- 低版本手机走工控机制作，无下载中/本地制作中状态

### 4. 视频播放器 (P0)
- 快捷操作: 连点(0~1s)快进/快退至下一杆/上一杆 | 长按1.5s→2.0x快进 | 左滑调亮度 | 右滑调音量
- 双击: 左/右→上一杆/下一杆 | 中心→暂停/播放
- 单击: 显示/隐藏完整控件栏（进度条/倍速/设置/分享/返回）
- 引导层: 前2次进入显示，引导层未关闭前控件不可操作
- 进度条: 设置中仅有**球谱开关**（无"球谱/时间"二选一）。球谱开启→球谱与时间进度条**绑定同步**显示；球谱关闭→仅显示时间进度条。时间进度条跟随控件显示/隐藏（点击屏幕出现，6s无操作消失）
- 倍速: 逐帧/0.25x/0.5x/1.0x(默认)/1.5x/2.0x，每次进入重置
- 分享: 第一期仅在**专业播放器内**（播放制作完成视频后）+ **我的视频页视频卡片右上角**，含微信/微信朋友圈/抖音/保存到相册
- 播放完自动回开头

### 5. 交手记录模块 (P0)
- 交手卡片/场卡片下方横滑视频卡片
- 展示状态: 待解锁/下载中/制作中/已解锁（不展示失败/过期）
- 排序: 待解锁>已解锁；类型: 我的单杆>对手单杆>精彩>失误>局
- 最多8个，超出显示【查看更多】
- 场卡片: 本场评级外显(⬆️⬇️超常/失常)，头像处标记场评级，X局X胜格式

### 6. 多语言 (P0)
- 国内版: 中文 + 英文

### 7. 埋点 (P0)
- 新增事件: user_signup/user_login/player_videocard/match_videocard/video_unlock_start/video_unlock_success/origin_download_success/fail/local_make_start/success/fail/video_play/video_share
- video_event表: from_type=3 表示App端

---

## 数据库关键表

> 连接: 121.40.243.17:3306 (supervisions库)
> 账号: linjiakun / 密码: Ljk@123456

### 视频业务核心表

| 表名 | 核心用途 | 关键状态/字段 |
|------|---------|-------------|
| video_client_status | App视频状态跟踪 | status: 0~8 共9个状态 |
| video_order | 视频解锁订单 | pay_channel(0~3), refund_status(0~3) |
| video_source | 视频原片地址 | url_video(腾讯云URL) |
| video_coupon_record | 视频券记录 | status: 0=有效, 1=已使用, 2=已过期 |
| video_event | 播放事件埋点 | from_type: 1=工控机, 2=小程序, 3=App |
| video_price | 定价规则 | discount(10=1折, 85=85折) |

### video_client_status 状态机（核心）

| 值 | 状态 | 小程序同步文案 |
|----|------|-------------|
| 0 | 待解锁 | 未明确（推测沿用现有样式） |
| 1 | 已解锁等待上传 | **制作中，请至App内查看** |
| 2 | 原片已上传 | 未明确（推测沿用现有样式） |
| 3 | 下载中 | 制作中，请至App内查看 |
| 4 | 下载失败 | 制作失败，请至App内查看 |
| 5 | 本地制作中 | 制作中，请至App内查看 |
| 6 | 制作失败 | 制作失败，请至App内查看 |
| 7 | 制作完成 | 制作成功，请至App内查看 |
| 8 | 已过期 | 已过期 |

> 小程序只展示5种文案，status 0/2未明确，status 1是"制作中"非"已解锁"。

### 用户数据关联规则

**查询路径**：手机号 → ten_user.id → ten_inning.left_id/right_id → 比赛数据

| 表名 | 说明 | 关联方式 |
|------|------|---------|
| ten_user | 用户基础信息 | 主表，phone/union_id 查询 |
| ten_inning | 局记录 | left_id / right_id 匹配 user_id |
| ten_score | 得分记录（聚合） | user_id 直接关联 |
| video_order | 视频订单 | user_id 直接关联 |
| video_list | 视频列表 | merchant_address_id 关联俱乐部 |
| video_combo_order | 套餐订单 | user_id 直接关联 |
| video_coupon_record | 视频券 | user_id 直接关联 |

**⚠️ 无数据的表**（斯诺克模式）：
- `ten_hit_info`: 击球信息表无记录（可能工控机未上传详细击球数据）
- `ten_participants`: 参赛人表无记录（斯诺克通过 ten_inning 的 left_id/right_id 直接关联）

### ️ 已知数据问题

| 问题 | 详情 |
|------|------|
| video_combo.price | 值为100/101/102分，与PRD的9.9/29.9/118.8元不符 |
| video_order.video_status | 大量为0，与注释不一致 |
| video_event.from_type | 只有2，缺工控机的1和App的3（App开发中） |
| discount=10 | 是1折非9折（折后百分比） |

---

## 测试用例规范

### 用例结构（9列）

| 列名 | 说明 |
|------|------|
| 用例编号 | TC-XXX，按模块连续编号，**禁止使用 -1 后缀**（如 TC-035-1），新增用例直接分配下一个连续编号 |
| 场景 | 功能场景分类 |
| 所属模块 | 所属功能模块 |
| 用例标题 | 必须突出预期结果，格式：`操作 + 预期结果` |
| 前置条件 | 执行用例前的必要状态，不混入操作步骤 |
| 操作步骤 | 按序号列举，每步原子操作 |
| 预期结果 | 按序号对应操作步骤，每步对应一个预期结果 |
| 优先级 | P0=核心流程 P1=重要功能 P2=边界/异常 |
| 执行结果 | 通过/失败/未执行 |
| 备注 | 补充说明 |

### 编写规则

1. **用例标题**：必须包含预期结果，阅读标题即可知道该用例验证什么
   - ✅ `微信授权成功后自动登录并同步小程序数据`
   - ❌ `测试微信登录`
2. **步骤与预期一一对应**：步骤N对应预期结果N，**条数必须完全相同**
3. **前置条件独立**：不混入操作步骤
4. **优先级划分**：P0=阻塞性 P1=影响体验 P2=边界/UI
5. **一条用例一个验证点**：不合并多个验证目标
6. **编号连续**：新增用例分配到下一个可用的连续编号，不使用子编号（禁止 TC-XXX-1 格式）
7. **Section 归属**：新增用例必须插入到对应功能模块的 Section 内，**禁止追加到文件末尾或其他无关 Section 下**
8. **行高自适应**：每行 TC 的行高必须根据内容条数自动计算，公式为 `max(25, 条数*18pt)`
9. **垂直居中**：每个单元格必须设置垂直居中（vertical=center）

### Section 顺序（每个 Sheet 内）

| 序号 | Section 名称 | 包含功能 |
|------|-------------|---------|
| ① | App架构 | 首页Tab、底部导航、全局交互 |
| ② | App登录及数据同步 | 微信/Apple/Google/Facebook登录、游客模式、注销 |
| ③ | 计分系统扫码登录 | App扫码登录计分系统 |
|  | 视频App本地制作 | 视频解锁、下载、制作、过期、渲染、小程序同步 |
| ⑤ | 我的视频模块优化 | 我的视频页、分类筛选、状态管理 |
| ⑥ | 交手记录模块 | 交手卡片、场卡片、视频卡片、评级 |
| ⑦ | 视频播放器 | 手势、进度条、倍速、引导层、分享、设置 |
| ⑧ | 首页模块 | 首页数据展示、在线状态、时间筛选 |
|  | 我的数据模块 | 个人数据、评级卡片、雷达图 |
| ⑩ | 排行榜模块 | 排行榜Tab、俱乐部榜/城市榜/评级榜 |
| ⑪ | 消息推送 | 推送通知（第二期） |
| ⑫ | 微信抖音分享 | 分享功能（第二期） |
| ⑬ | 多语言 | 中英文切换、文案完整性、样式适配 |
| ⑭ | 埋点 | 埋点事件上报、字段完整性 |
| ⑮ | 兼容性测试 | 系统版本、机型、屏幕尺寸适配 |

### 冒烟测试规范

放在独立 Sheet「冒烟测试」中，编号 SM-001 起连续编排。

**编写原则**：
1. 按需求维度覆盖，端到端流程优先
2. **冒烟测试用例不受步骤数量限制**（可超过12步）
3. 全部P0优先级
4. 步骤与预期严格一一对应

### 三平台同步规则

- 安卓/国内iOS/海外iOS 三个 Sheet 的测试用例需保持同步
- 需求不区分平台的功能（视频制作、播放器等），三个 Sheet 用例内容完全一致
- 按地区区分的功能（登录方式、支付等），各 Sheet 写各自平台的用例

### Excel 表格格式规范

**TC 行样式**：字体微软雅黑 10pt，背景色 FFFCE4EC，边框 thin，垂直居中 + 自动换行

**Section Header 样式**：微软雅黑 10pt 加粗，白色字体，深蓝背景 FF2F5496，**合并 A:J 列**

**执行结果列（第9列）**：
- 下拉选项：通过 / 失败
- 条件格式：通过→浅绿+深绿粗体，失败→浅红+深红粗体
- 预设范围 I2:I10000，新增用例自动带上下拉和颜色联动

---

## 用例生命周期管理

### 核心修复

`insert_rows` / `delete_rows` 不调整合并范围，操作前必须取消所有合并，操作后重新合并。

### Excel 操作用 `/test-case-ops` skill

涉及 insert_rows / delete_rows 等 Excel 操作时，必须使用 `/test-case-ops` skill 中的代码模板。

### 新增用例

1. 找到目标 Section 内按功能逻辑应插入的位置
2. 新用例编号 = 该位置原用例的编号
3. 原位置及后续所有用例编号依次 +1

### 删除用例

1. 删除目标行
2. 后续所有 TC 编号 -1
3. 重新合并所有 Section

### 修改用例

- 直接修改目标单元格内容
- **不改变编号**（除非修改导致用例归属 Section 变化）

### 批量变更顺序

1. **先删除** → 重新编号
2. **再修改** → 调整内容
3. **最后新增** → 插入并重新编号

### 操作后必须验证

- [ ] 新行无合并单元格
- [ ] 后续行内容完整（场景、标题不为空）
- [ ] Section Header 合并正确
- [ ] 编号连续无断号
- [ ] 样式与已有行一致

### 文件保存

如遇 `PermissionError`（WPS 未关闭），保存为 `原文件名_temp.xlsx`，提示用户关闭 WPS 后替换。

---

## 第一期测试分工

| 人员 | 负责模块 | 说明 |
|------|---------|------|
| **仕杰** | 架构兼容 / 视频制作 / 交手视频卡片 | 含视频过期、视频卡片UI |
| **琴姐** | 视频播放器 / 埋点 | 含快捷操作、进度条、倍速、引导、音频 |
| **梓聪** | 登录及数据同步 / 我的视频 / 多语言 | 含扫码登录、分类筛选 |

**不编写用例**: 个人数据可视化（第二期）、消息推送（第二期）、支付/退款（第二期）、播放器筛选功能（第二期）、微信长图分享（第二期）

---

## 待确认项

- [ ] iOS/Android 最低系统版本要求
- [ ] 可用存储空间、内存要求
- [ ] 带/不带比分条视频每分钟制作耗时目标
- [ ] 制作方式判断逻辑（本地 vs 工控机的设备阈值）
- [x] 48h vs 72h 过期时间 → **已确认48h**（UI稿72h需产品通知UI团队修正）
- [x] 我的视频Tab命名 → **制作完成/制作失败**
- [x] 第一期分享范围 → **仅专业播放器内 + 我的视频页右上角，有抖音**
- [x] 播放器筛选功能 → **第二期**，第一期不考虑
- [x] 海外iOS登录方式 → **Apple + Google + Facebook** 三种，第一期
- [x] 相册权限弹窗触发时机 → **解锁视频成功后**，**我的视频页面制作完成视频右上角分享按钮**
- [x] 支付/退款模块 → **第二期**，第一期不编写用例
- [x] 解锁后弹窗顺序 → **先"视频已解锁"→再"相册权限"**
- [x] Google/Facebook数据同步 → **不涉及**，仅微信登录与小程序同步数据
- [x] 底部优惠券横幅 → **第一期不考虑**

---

## 技术要点（测试相关）

- 微信 UnionID 机制实现小程序↔App账号打通
- 视频原片存储: 腾讯云 (vod2.myqcloud.com)
- 本地制作 = 素材(原片片段) + 数据(比分/头像/时间戳) 下载后App端合成
- 推送服务: 极光推送
- 分享SDK: 微信SDK + 抖音开放平台
