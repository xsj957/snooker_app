# CLAUDE.md - 斯诺克大师 App 测试规范

## 知识库索引

| 类别 | 路径 | 说明 |
|------|------|------|
| 第一期需求 | `_knowledge/业务模块/第一期需求.md` | 登录、视频制作、我的视频、播放器、交手记录、多语言、埋点 |
| 第二期需求 | `_knowledge/业务模块/第二期需求.md` | 支付、个人数据可视化、微信抖音分享、消息推送 |
| 技术知识 | `_knowledge/技术知识/` | 数据库、API、测试用例规范、抓包方法 |

**规则**：写用例前，先 Read 对应迭代的业务模块文档。不要凭记忆。

---

## 项目概述

**产品名称**: 斯诺克大师 App (Snooker Master)
**测试范围**: 国内版App

**迭代目录**:
- `iter-v1.0.0-第一期/` — 登录、视频制作、我的视频、播放器、交手记录、多语言、埋点
- `iter-v1.0.1-第二期/` — 支付模块、个人数据可视化、微信抖音分享、消息推送

---

## 待确认项

- [x] iOS/Android 最低系统版本 → **iOS 15 / Android 10.0**
- [x] 可用存储空间 → **制作视频时≥视频大小+200MB**
- [x] 48h vs 72h 过期 → **已确认48h**
- [x] 我的视频Tab → **有效视频/失效视频**
- [x] 相册权限弹窗 → **解锁视频成功后** + **我的视频分享按钮**
- [x] 解锁后弹窗顺序 → **先"视频已解锁"→再"相册权限"**
- [x] Google/Facebook同步 → **不涉及**，仅微信登录同步小程序
- [ ] 带/不带比分条视频每分钟制作耗时目标
- [ ] 制作方式判断逻辑（本地 vs 工控机的设备阈值）
- [ ] 视频压缩后每分钟大小及清晰度目标
- [ ] 个人数据页是否只可查看近一年数据

---

## 技术要点

- 微信 UnionID 实现小程序↔App账号打通
- 视频原片存储: 腾讯云 (vod2.myqcloud.com)
- 本地制作 = 素材 + 数据下载后 App 端合成
- 推送: 极光推送 | 分享: 微信SDK + 抖音开放平台

### 视频券每日免费发放规则
- 用户每天完成第一场比赛后，系统自动发放1张免费视频券
- 该券仅可用于解锁1个视频（不限视频类型：单杆/集锦/局视频均可）
- 每天最多1张免费券，当天无论再打多少场比赛均不再免费发放

### 视频解锁方式（两种互斥，写用例必须遵守）

> **核心理解**：现金解锁 ≠ 产生视频券，视频券解锁 = 必须先用现金购买套餐获得券。两种解锁方式的退款规则完全不同。

| 解锁方式 | 资金来源 | 是否产生视频券 | 退款规则 |
|---------|---------|--------------|---------|
| **现金解锁** | 微信支付（安卓）/ 苹果 IAP（iOS） | ❌ **不产生视频券** | ✅ 支持**现金退款**（原路退回） |
| **视频券解锁** | 使用视频券账户里的券（1张券=解锁1个视频） | ✅ 券来源只能是：① 购买视频券套餐；② 每日免费券；③ 运营发放活动券 | ✅ 仅支持**退券**（将已使用的券退回账户），❌ **不支持现金退款**，❌ 不支持退套餐金额 |

**业务规则**：
- 视频券只能通过"购买视频券套餐"获得，**不能**通过直接现金购买单个视频的券
- 现金解锁是"一手交钱一手交货"的独立支付，与券账户无关联
- 用户即使账户下同时有券和现金，解锁某个视频时也只能选择**其中一种**方式，不能混合支付
- 退款时按**原路返回**原则：现金付的退现金，券付的退券

**数据库区分方式**（`video_order` 订单表）：

| 字段 | 现金解锁 | 视频券解锁 |
|------|---------|-----------|
| `combo_order_id` | **IS NULL 或 = 0** | **IS NOT NULL 且 > 0**（关联 `video_combo_order.id`） |
| `amount` | 实际支付金额（分） | 视频原价（非实际支付，因为是用券抵扣） |
| `pay_status` | 支付成功 | 支付成功（用券支付也算"支付成功"） |
| `refund_status` | 支持 0/1/2/3 正常流转 | 同样有该字段，但**退款逻辑应退券而非现金** |

**查询 SQL**：
```sql
-- 查询用户的现金解锁视频（支持现金退款）
SELECT * FROM video_order WHERE user_id = '{user_id}' AND pay_status = '支付成功' AND (combo_order_id IS NULL OR combo_order_id = 0);

-- 查询用户的券解锁视频（仅支持退券）
SELECT * FROM video_order WHERE user_id = '{user_id}' AND pay_status = '支付成功' AND combo_order_id IS NOT NULL AND combo_order_id > 0;

-- 通过 combo_order_id 追溯券来源（哪个套餐订单）
SELECT vo.id, vo.video_id, vo.amount, vco.combo_id, vco.end_time
FROM video_order vo
JOIN video_combo_order vco ON vo.combo_order_id = vco.id
WHERE vo.user_id = '{user_id}';
```

**测试用例编写规则**：
- 写退款用例时，**必须区分解锁方式**而非区分支付方式（微信/IAP）
- "视频券解锁"的退款用例：前置条件是用户通过购买套餐获得的券解锁了视频，预期结果必须是"退券"而非"退款"
- "现金解锁"的退款用例：前置条件是用户直接通过微信/IAP支付解锁了视频，预期结果才是"现金退款"
- 不能出现"视频券退款"这种混淆表述，应该说"视频券解锁的视频退款"（指退券）或"现金解锁的视频退款"（指退现金）

### 推送事件触发条件（写用例必须遵守）
- **比赛结果通知**：比赛结束由**工控机/计分系统**产生，不依赖App运行状态。App被杀进程后仍可触发推送
- **视频制作结果通知**：视频制作**至少需要App在后台运行**，杀进程后无法触发制作完成事件
- 因此"杀进程后仍能收到推送"用例，触发事件只能用**比赛结束**，不能用视频制作完成/失败

### client_id 逻辑
- App 安装后生成的 UUID，卸载重装会变化
- 查询路径: `user_id → video_order.video_id → video_client_status.client_id`
- 日志提取: `adb logcat | grep "DIO.*data:" -A1 | grep -i "clientId"`
